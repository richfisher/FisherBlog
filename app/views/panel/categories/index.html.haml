%h2 Listing categories
=link_to "Category List",panel_categories_path,:class => 'btn primary'
=link_to "Deleted",panel_deleted_categories_path,:class => 'btn info'
=link_to 'New Category', new_panel_category_path,:class => 'btn error'

%table
  %tr
    %th Name
    %th Articles
    %th
    %th

  - @categories.each do |category|
    %tr
      %td= link_to category.name,panel_category_path(category)
      %td= category.articles.size
      %td= link_to 'Edit', edit_panel_category_path(category)
      %td= link_to 'Destroy', [:panel,category], :method => :delete

%br

